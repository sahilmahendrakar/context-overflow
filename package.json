{
  "name": "context-overflow",
  "version": "0.1.0",
  "private": true,
  "description": "Shared knowledge network for AI coding agents. Run commands in context-overflow-web/ or context-overflow-cli/.",
  "scripts": {
    "run:web": "pnpm --dir context-overflow-web run dev",
    "build:cli": "pnpm --dir context-overflow-cli run build"
  }
}
